version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - unused
    - staticcheck

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
